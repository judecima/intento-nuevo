name: Optimizer Mid-Type High-Repeat P3 vs Full40

on:
  push:
    branches:
      - research/perfv1-master-win-mining-20260921
    paths:
      - "research/optimizer/master-generation/mid-types-high-repeat-active-prep.cjs"
      - "research/optimizer/master-generation/mid-types-high-repeat-benchmark.cjs"
      - ".github/workflows/optimizer-mid-types-high-repeat-p3.yml"

permissions:
  contents: read
  actions: read

env:
  MINING_RUN_ID: "35662373127"

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Download mining shards
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          mkdir -p research/optimizer/master-generation
          rm -rf /tmp/master-win-shards
          mkdir -p /tmp/master-win-shards
          gh run download "$MINING_RUN_ID" --repo "$GH_REPO" --pattern "perfv1-master-win-mining-shard-*" --dir /tmp/master-win-shards
          find /tmp/master-win-shards -name 'master-win-mining-shard-*.json' -exec cp {} research/optimizer/master-generation/ \;
          count=$(find research/optimizer/master-generation -maxdepth 1 -name 'master-win-mining-shard-*.json' | wc -l)
          echo "downloaded shards=$count"
          [ "$count" -eq 32 ]
      - name: Build cohort
        run: node research/optimizer/master-generation/mid-types-high-repeat-active-prep.cjs
      - uses: actions/upload-artifact@v4
        with:
          name: mid-types-high-repeat-active-manifest
          path: research/optimizer/master-generation/mid-types-high-repeat-active-manifest.json
          if-no-files-found: error
          retention-days: 7

  shard:
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        shard: [0,1,2,3,4,5,6,7]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - run: npm ci
      - run: npm run optimizer:rust:build
      - uses: actions/download-artifact@v4
        with:
          name: mid-types-high-repeat-active-manifest
          path: research/optimizer/master-generation
      - name: Benchmark shard ${{ matrix.shard }}
        env:
          SHARD_INDEX: ${{ matrix.shard }}
          SHARD_TOTAL: 8
          RUST_LEGACY_DEBUG_ERRORS: "1"
        run: node research/optimizer/master-generation/mid-types-high-repeat-benchmark.cjs shard
      - uses: actions/upload-artifact@v4
        with:
          name: mid-types-high-repeat-benchmark-shard-${{ matrix.shard }}
          path: research/optimizer/master-generation/mid-types-high-repeat-benchmark-shard-${{ matrix.shard }}.json
          if-no-files-found: error
          retention-days: 7

  report:
    needs: shard
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/download-artifact@v4
        with:
          pattern: mid-types-high-repeat-benchmark-shard-*
          path: research/optimizer/master-generation
          merge-multiple: true
      - name: Build report
        env:
          SHARD_TOTAL: 8
        run: node research/optimizer/master-generation/mid-types-high-repeat-benchmark.cjs report
      - uses: actions/upload-artifact@v4
        with:
          name: mid-types-high-repeat-benchmark-results
          path: research/optimizer/master-generation/mid-types-high-repeat-benchmark-results.json
          if-no-files-found: error
          retention-days: 7
